const express = require('express');

const ProuctService = require('./../services/product.service');

const router = express.Router();
const service = new ProductsService();

router.get('/', middleware, async function(request, response, next){

    try{
        //
    } catch(error){

    }

}
);

module.export = router;